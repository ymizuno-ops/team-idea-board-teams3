# チーム演習：個人の変更をチームの成果として届ける

## ゴール

個人で実装した変更をチームの成果へつなげるために、方針を揃え、変更を相互に確認し、公開を共同で判断する一連の流れを体験します。

全員が小さな機能を1つ実装し、別の参加者のPull Requestを1つレビューします。
最後に、全員の変更をひとつのWebアプリとして公開し、開発を通じて得た学びを共有します。

## ワーク全体の時間

このワークは、チームリポジトリの準備20分と開発ワーク60分で構成します。

```text
チームリポジトリの準備：20分
開発、レビュー、公開：60分
合計：80分
```

最初に[TEAM_SETUP.md](./TEAM_SETUP.md)を開き、チーム用リポジトリ、CI、GitHub Pages、Ruleset、Issueを設定します。
「開発ワークの開始条件」をすべて満たしてから、以下の60分を開始します。

## ルール

- 1人1 Issue、1 Branch、1 Pull Requestで進める
- 実装前に、担当範囲と完成条件をチームで共有する
- `main`へ直接Pushしない
- 担当Issueの範囲を越えて変更しない
- 自分のPull Requestを自分で承認、マージしない
- レビューではコードだけでなく、実際の画面も確認する
- マージ前に、チーム全員で公開してよいか確認する

## 開発ワーク60分の進め方

| 時間 | 内容 |
|---|---|
| 0〜8分 | チームで担当Issue、実装方針、レビュー相手、完成条件を確認する |
| 8〜28分 | 各自がBranchを作り、AIと機能、テストを実装する |
| 28〜33分 | PushしてPull Requestを作成する |
| 33〜43分 | 隣の参加者のPull Requestをレビューする |
| 43〜50分 | 指摘を修正し、再確認、承認する |
| 50〜55分 | チームで公開可否を判断し、Pull Requestを順番にマージする |
| 55〜60分 | 公開URLで全機能を確認し、実装内容と学びを共有する |

## 1. チームで方針を揃える

担当Issueを読み、次の内容をチームで確認します。

- 利用者は何ができるようになるか
- 何を確認できたら完成か
- 主に変更するファイルはどこか
- ほかの担当者の変更と影響し合う可能性があるか
- Pull Requestをマージしてよいと判断する条件は何か

各自が次の形式で、担当する変更を30秒で説明します。

```text
私はTicket ○を担当します。
○○を変更し、○○という操作とテストで確認します。
ほかの変更へ影響しそうな箇所は○○です。
```

## 2. 個人で実装する

```bash
git switch main
git pull
git switch -c feature/issue番号-短い機能名
```

担当Issueとチームで確認した方針をClaude Codeへ渡します。

```text
担当するGitHub Issue、CLAUDE.md、チームで確認した実装方針を確認してください。
受け入れ条件を満たすための変更範囲とテスト方法を整理してください。
確認後、担当Issueの範囲だけを実装してください。
既存のit.todoを実際のテストへ置き換え、npm run checkまで実行してください。
最後に、AIが提案した内容と、人間が判断した内容を分けて説明してください。
```

## 3. Pull Requestを作成する

```bash
git add .
git commit -m "feat: 実装した機能を簡潔に記載"
git push -u origin 現在のブランチ名
gh pr create --fill
```

Pull Requestテンプレートを、自分の言葉で埋めてください。
変更内容と確認方法に加え、AIの提案をどのように判断したかを書きます。

## 4. 隣の人をレビューする

3人ならA→B→C→A、4人ならA→B→C→D→Aの順でレビューします。

`REVIEW_GUIDE.md`に沿って、最低1件の確認コメントを残してください。
必要であれば、実装者が2分以内で画面を共有し、実際の操作を見せます。

問題がなければApprove、問題があればRequest changesを選びます。

## 5. チームで公開を判断する

全員のCIとレビューが完了したら、各自が次の内容を説明します。

```text
私は○○を実装しました。
この操作をすると、画面が○○に変わります。
○○というテストと画面操作で確認しました。
AIは○○を提案し、私は○○と判断しました。
```

チームは、次の条件が揃ったPull Requestだけを公開可能と判断します。

- Issueの受け入れ条件を満たしている
- CIが成功している
- 別の参加者によるレビューが完了している
- 実装者が変更内容と確認方法を説明できる

公開可能と判断したPull Requestを1本ずつ`main`へマージします。
最後のマージ後、GitHub Actionsの「Deploy to GitHub Pages」が成功するまで待ちます。

## 6. 公開結果と学びを共有する

公開URLで全員分の機能が動くことを確認します。

最後に、各自が次のうち1つを共有します。

- AIへ任せてうまくいったこと
- 人間が判断する必要があったこと
- 他者のレビューで気づいたこと
- 次に改善したいこと

## 7. 時間が余った場合は次の改善をIssueにする

必須Issueの実装、レビュー、マージが完了したチームは、公開されたアプリを全員で操作します。
機能の追加やデザインの改善について話し合い、実装を始める前に新しいGitHub Issueを作成してください。

新しいIssueには、最低限、次の内容を記載します。

- なぜ改善するのか
- 利用者が何をできるようになるのか
- 何を確認できたら完成か
- 今回は実装しないこと

担当者とレビュー相手を決め、必須Issueと同じくBranch、Pull Request、CI、他者レビューを経て`main`へマージします。
